import { createText } from "@shopify/restyle";

import DynamicView from "./DynamicView";

import { Theme } from "@/app/theme";

const DynamicText = createText<Theme>();

export { DynamicView, DynamicText };
